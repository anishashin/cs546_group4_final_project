const userData = require('./users');
const foodData = require('./foods');
const savedPlateData = require('./savedPlates');
const commentData = require('./comments');

module.exports = {
  users: userData,
  foods: foodData,
  savedPlates: savedPlateData,
  comments: commentData
};